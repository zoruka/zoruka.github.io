import React from 'react';
import { Layout } from '../components';

export const SkillsComposition: React.FC = () => {
  return (
    <Layout theme="dark">
      <h1>Skills</h1>
    </Layout>
  );
};
